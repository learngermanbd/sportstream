<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Step 1.5 will replace this with Material 3 dark theme + glassmorphism. -->
    <style name="Theme.SportStream" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
